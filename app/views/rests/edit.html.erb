<h1>Editing Restaurant</h1>

<%= form_for(@rest) do |f| %>
  <% if @rest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rest.errors.count, "error") %> prohibited this Restaurant from being saved:</h2>

      <ul>
      <% @rest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', user_rests_path(:user_id) %>